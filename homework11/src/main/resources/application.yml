spring:
  data:
    mongodb:
      #uri: mongodb://localhost
      #port: 27017
      port: 0
      database: librarydb

logging:
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{35} - %msg%n"
  level:
    root: info